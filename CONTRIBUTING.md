# Katkı

1. `ai/context/` dosyalarını güncel tutun (mimari/API değişince).
2. Yorumlar Türkçe; kod İngilizce isimlendirme (`snake_case`).
3. `.env` asla commit etmeyin.
4. Claude yalnızca `ai/context/token-policy.md` kurallarına uygun çağrılsın.
5. PR öncesi: `make lint` (opsiyonel), manuel smoke `GET /health`.

Detay: [`ai/README.md`](ai/README.md)
