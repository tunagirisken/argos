# Keşif sayfası (frontend)

**Durum:** Uygulandı (`/discovery`).

Backend: `GET /api/discovery/latest`, `POST /api/discovery/scan`.

Frontend: `frontend/src/features/discovery/DiscoveryPage.tsx` — fırsat listesi, tarama, hisse detayına link (izleme modu).
