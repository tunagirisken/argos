# Admin girişinde otomatik backend/.env oluşturmak için:
#   cp backend/.env.bootstrap.example backend/.env.bootstrap
#   Değerleri doldurun (bu dosya .gitignore'da — commit ETMEYİN)
#
# Admin şifresi ayrı: sunucuda ARGOS_ADMIN_PASSWORD ortam değişkeni
# veya backend/.env içinde (bootstrap sonrası)

LLM_PROVIDER=gemini
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
FIRECRAWL_API_KEY=
EXA_API_KEY=
SENTRY_DSN=
LOG_LEVEL=INFO
ENV=development
